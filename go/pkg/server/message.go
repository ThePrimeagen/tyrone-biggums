package server

type Message struct {
    id uint
    message string
}


/*
func (m *Message) fromMessage(message string) *Message {
    return Message {
        id: m.id,
        message,
    }
}
*/

